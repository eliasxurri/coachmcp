# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
terraform/build/
# Config del backend: lleva el bucket propio de cada quien.
backend.hcl
# Los planes guardados son artefactos binarios y llevan dentro los
# atributos de todos los recursos.
tfplan
*.tfplan

# Archivo original del proyecto, ya desempaquetado en terraform/ y lambda/
files.zip

# Python
__pycache__/
*.py[cod]
.venv/

# Resultados descargados de Athena y respuestas de invocaciones locales
*.csv
respuesta.json

# Configuración local de herramientas
.claude/
.codex/
.agents/

# Local
.DS_Store

# Contexto estratégico personal, no versionar
company-context.md

# Paquete de despliegue del servidor MCP remoto (lo arma construir.sh)
mcp-server/build/
